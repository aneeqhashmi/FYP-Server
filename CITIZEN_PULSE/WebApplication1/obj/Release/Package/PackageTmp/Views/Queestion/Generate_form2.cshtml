@*@model  IEnumerable<WebApplication1.Models.Question>

    @{
        ViewBag.Title = "Generate_form2";
    }
    <h3>@ViewBag.Message</h3>

    <h2>Generate_form2</h2>
    <table class="table">
        <tr>


            <th>
                @Html.DisplayNameFor(model => model.Questionid)

            </th>

            <th>
                @Html.DisplayNameFor(model => model.Q_text)

            </th>
        </tr>

        @foreach (var item in Model)
        {

            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.Questionid)
                </td>

                <td>
                        @Html.DisplayFor(modelItem => item.Q_text)
                </td>


            </tr>
        }

    </table>*@
@model  WebApplication1.Models.Survey
@using WebApplication1.Models;

@{
    ViewBag.Title = "Generate_form3";
}

<h2 class="back">Survey Form</h2>
<h3 class="back">@Model.SurveyId</h3>
<h3 class="back">@Model.Name</h3>

<table class="table">





    @foreach (Question q in Model.Questions)
    {
        <tr>
          
            <td>
               <b> @Html.DisplayFor(modelItem => q.Q_text)</b>
            </td>
        </tr>
        foreach (option o in q.options)
        {
            <tr>
                <td>
                    <input id="Radio1-@o.optionId" type="radio"  value="@o.optionId" name="options"  /><b> @Html.DisplayFor(modelItem => o.op_text)</b>
                </td>

            </tr>
        }
    }

</table>


<div>
    @Html.ActionLink("Back to List", "Welcome", "user")


</div>